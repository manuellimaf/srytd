<configuration scan="true">

  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>/var/log/srytd.log</file>

    <encoder immediateFlush="true">
		<pattern>%date{yyyy-MM-dd HH:mm:ss} %-5level [%.-35thread][%X{USER}][%X{PROCESS_ID}] %logger{5} - %msg %n</pattern>
    </encoder>

    <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
        <fileNamePattern>/var/log/srytd.%i.log</fileNamePattern>
        <minIndex>1</minIndex>
        <maxIndex>5</maxIndex>
    </rollingPolicy>

  </appender>

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder immediateFlush="true">
		<pattern>%date{yyyy-MM-dd HH:mm:ss} %-5level [%.-35thread][%X{user}][%X{processId}] %logger{5} - %msg %n</pattern>
    </encoder>
  </appender>

  <logger name="org.eclipse.jetty" level="INFO" />
  <logger name="org.hibernate" level="INFO" />
  <logger name="org.hibernate.type" level="TRACE" />
	
  <root level="DEBUG">
    <appender-ref ref="FILE" />
    <appender-ref ref="STDOUT" />
  </root>
</configuration>
